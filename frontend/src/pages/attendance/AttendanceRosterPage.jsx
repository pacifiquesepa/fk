export default function AttendanceRosterPage({ students, statuses, setStatus, className, date, onBack, onNext }) {
    return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-display text-xl font-bold text-slate-800">Student list</h2><p className="mt-1 text-xs text-slate-500">Class: {className} · Date: {date}</p><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[580px] text-left text-xs"><thead className="border-b border-slate-200 text-slate-400"><tr><th className="p-3">#</th><th className="p-3">Student name</th><th className="p-3">Gender</th><th className="p-3">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{students.map((student, index) => <tr key={student.id}><td className="p-3">{index + 1}</td><td className="p-3 font-bold text-slate-700">{student.fullName}</td><td className="p-3">{student.gender || '-'}</td><td className="p-3"><select value={statuses[student.id] || 'present'} onChange={(event) => setStatus(student.id, event.target.value)} className="rounded-lg border border-slate-200 px-3 py-2 text-xs font-bold"><option value="present">Present</option><option value="absent">Absent</option><option value="late">Late</option><option value="excused">Excused</option></select></td></tr>)}</tbody></table></div><div className="mt-6 flex gap-3"><button onClick={onBack} className="rounded-xl border border-slate-200 px-5 py-3 text-xs font-bold text-slate-600">Back</button><button disabled={!students.length} onClick={onNext} className="rounded-xl bg-emerald-700 px-5 py-3 text-xs font-bold text-white disabled:opacity-50">Review & Save</button></div></section>;
}
