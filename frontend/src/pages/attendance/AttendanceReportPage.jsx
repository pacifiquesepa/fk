import { Printer, Save } from 'lucide-react';

export default function AttendanceReportPage({ students, statuses, comments, setComment, className, date, saving, onBack, onSave }) {
    const print = () => window.print();
    return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm print:border-0 print:shadow-none"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-display text-xl font-bold text-slate-800">Attendance report</h2><p className="mt-1 text-xs text-slate-500">Class: {className} · Date: {date}</p></div><button onClick={print} className="flex items-center gap-2 rounded-xl border border-slate-200 px-4 py-3 text-xs font-bold text-slate-700 print:hidden"><Printer size={16} />Print</button></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[650px] text-left text-xs"><thead className="border-b border-slate-200 text-slate-400"><tr><th className="p-3">#</th><th className="p-3">Student name</th><th className="p-3">Status</th><th className="p-3">Excused comment</th></tr></thead><tbody className="divide-y divide-slate-100">{students.map((student, index) => <tr key={student.id}><td className="p-3">{index + 1}</td><td className="p-3 font-bold text-slate-700">{student.fullName}</td><td className={`p-3 font-bold ${statuses[student.id] === 'present' ? 'text-green-700' : statuses[student.id] === 'absent' ? 'text-red-700' : statuses[student.id] === 'late' ? 'text-pink-700' : 'text-yellow-700'}`}>{statuses[student.id]}</td><td className="p-3">{statuses[student.id] === 'excused' ? <input value={comments[student.id] || ''} onChange={(event) => setComment(student.id, event.target.value)} placeholder="Comment required" className="rounded-lg border border-slate-200 px-3 py-2 print:border-0" /> : '-'}</td></tr>)}</tbody></table></div><div className="mt-6 flex gap-3 print:hidden"><button onClick={onBack} className="rounded-xl border border-slate-200 px-5 py-3 text-xs font-bold text-slate-600">Back</button><button disabled={saving} onClick={onSave} className="flex items-center gap-2 rounded-xl bg-emerald-700 px-5 py-3 text-xs font-bold text-white disabled:opacity-50"><Save size={16} />{saving ? 'Saving...' : 'Save Attendance'}</button></div></section>;
}
