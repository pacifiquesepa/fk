import { CalendarDays } from 'lucide-react';

export default function AttendanceSetupPage({ classes, classId, date, setClassId, setDate, onNext, onBack }) {
    return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-display text-xl font-bold text-slate-800">Choose class and date</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-xs font-bold text-slate-600">Select Class / Group<select value={classId} onChange={(event) => setClassId(event.target.value)} className="rounded-xl border border-slate-200 px-3 py-3 text-sm"><option value="">Select class</option>{classes.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="grid gap-2 text-xs font-bold text-slate-600">Date<input type="date" value={date} onChange={(event) => setDate(event.target.value)} className="rounded-xl border border-slate-200 px-3 py-3 text-sm" /></label></div><div className="mt-5 flex gap-3"><button onClick={onBack} className="rounded-xl border border-slate-200 px-5 py-3 text-xs font-bold text-slate-600">Back</button><button disabled={!classId} onClick={onNext} className="flex items-center gap-2 rounded-xl bg-cyan-700 px-5 py-3 text-xs font-bold text-white disabled:opacity-50"><CalendarDays size={16} />Load Students</button></div></section>;
}
