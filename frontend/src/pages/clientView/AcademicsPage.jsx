import ClientContentPage from './ClientContentPage';
import { BookOpen, Download, Folder, Search } from 'lucide-react';
import { useEffect, useMemo, useState } from 'react';
import ClientNavbar from './ClientNavbar';
import ClientFooter from './ClientFooter';
import api from '../../lib/api';

const fallback = [
	{ id: 'year-1', yearName: 'Year 1', title: 'Early years foundation', subjectName: 'General learning', description: 'Language, play, confidence and social development.' },
	{ id: 'year-2', yearName: 'Year 2', title: 'Primary learning foundation', subjectName: 'Core subjects', description: 'A strong academic foundation with attentive teaching.' },
	{ id: 'year-3', yearName: 'Year 3', title: 'Holistic development', subjectName: 'Life skills', description: 'Morals, citizenship and practical skills woven into learning.' },
];

export default function AcademicsPage({ language, onLanguageChange, onLogin, onNavigate, t }) {
	const [items, setItems] = useState([]); const [selectedYear, setSelectedYear] = useState('all'); const [query, setQuery] = useState('');
	useEffect(() => { let active = true; api.get('/curriculum').then(({ data }) => active && setItems(data.curriculum || [])).catch(() => {}); return () => { active = false; }; }, []);
	const source = items.length ? items : fallback;
	const years = ['all', ...new Set(source.map((item) => item.yearName))];
	const visible = useMemo(() => source.filter((item) => (selectedYear === 'all' || item.yearName === selectedYear) && `${item.title} ${item.description} ${item.subjectName || ''}`.toLowerCase().includes(query.toLowerCase())), [source, selectedYear, query]);
	return <div className="min-h-screen bg-[#f7faf9] text-slate-800"><ClientNavbar language={language} onLanguageChange={onLanguageChange} onLogin={onLogin} onNavigate={onNavigate} t={t} /><main className="pt-[72px]"><section className="bg-[#17333d] px-5 py-20 text-white sm:px-8 lg:py-28"><div className="mx-auto max-w-6xl"><p className="text-xs font-bold uppercase tracking-[0.22em] text-teal-200">Forever King Academy</p><h1 className="mt-4 max-w-3xl font-display text-4xl font-bold leading-tight sm:text-6xl">Curriculum</h1><p className="mt-5 max-w-2xl text-sm leading-7 text-slate-300">Explore learning resources by year, subject and stage. Our curriculum grows with every learner.</p></div></section><section className="mx-auto grid max-w-6xl gap-10 px-5 py-12 sm:px-8 lg:grid-cols-[240px_1fr] lg:py-20"><aside className="h-fit overflow-hidden rounded-2xl border border-slate-200 bg-white lg:sticky lg:top-24"><div className="border-b border-slate-100 px-5 py-4 text-xs font-bold uppercase tracking-wider text-slate-400">Years</div>{years.map((year) => <button key={year} onClick={() => setSelectedYear(year)} className={`flex w-full items-center gap-3 border-b border-slate-100 px-5 py-4 text-left text-sm font-semibold capitalize ${selectedYear === year ? 'bg-[#e9eef3] text-cyan-800' : 'text-slate-500 hover:bg-slate-50'}`}><Folder size={17} />{year === 'all' ? 'All curriculum' : year}</button>)}</aside><div><div className="flex flex-col gap-4 border-b border-slate-200 pb-6 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-cyan-700">Academic resources</p><h2 className="mt-2 font-display text-3xl font-bold text-slate-800">Learning by year</h2></div><label className="flex items-center gap-2 rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-slate-400"><Search size={16} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search curriculum" className="w-full bg-transparent text-xs outline-none sm:w-48" /></label></div><div className="mt-8 grid gap-5 md:grid-cols-2">{visible.map((item) => <article key={item.id} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start justify-between gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-cyan-50 text-cyan-700"><BookOpen size={21} /></span><span className="rounded-full bg-slate-100 px-3 py-1 text-[10px] font-bold text-slate-500">{item.yearName}</span></div><h3 className="mt-6 font-display text-lg font-bold text-slate-800">{item.title}</h3>{item.subjectName && <p className="mt-2 text-xs font-bold text-cyan-700">{item.subjectName}</p>}<p className="mt-3 text-xs leading-6 text-slate-500">{item.description}</p>{item.fileUrl && <a href={item.fileUrl} target="_blank" rel="noreferrer" className="mt-5 flex items-center gap-2 text-xs font-bold text-cyan-700">Open resource <Download size={15} /></a>}</article>)}</div>{!visible.length && <p className="py-20 text-center text-sm text-slate-400">No curriculum resources found.</p>}</div></section></main><ClientFooter onLogin={onLogin} onNavigate={onNavigate} /></div>;
}
